pulse_on:
  sequence:
  - service: switch.turn_on
    data_template:
      entity_id: "{{param}}"
  - service: switch.turn_off
    data_template:
      entity_id: "{{param}}"


pulse_off:
  sequence:
  - service: switch.turn_on
    data_template:
      entity_id: "{{param}}"
  - service: switch.turn_off
    data_template:
      entity_id: "{{param}}"